# Terraform working dirs and state — never commit
.terraform/
*.tfstate
*.tfstate.*
crash.log

# Local variable files may carry subscription IDs / secrets. Examples are kept.
*.tfvars
!*.tfvars.example
.claude/
.terraform.lock.hcl

# Local scratch (CLI scripts, throwaway)
tmp/

# Prepare-module handback files — credentials, never commit
cielara-key.json
cielara-creds.json
